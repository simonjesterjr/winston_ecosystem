# Session: RTX 3090 + Ollama CUDA on sawtooth-ai

**When:** 2026-09-16 ~19:00–20:00 MT  
**Actors:** John + Chief of Staff  
**Host:** sawtooth-ai (Pop!_OS 24.04, rootless Podman 5.8)

## Goal

Bring the new RTX 3090 online for Winston AI profile (`ollama`) and leave a durable trail in ecosystem.

## Outcome

**Success (workaround path).** Ollama discovers CUDA on the 3090 and offloads inference layers. CDI remains broken under rootless Podman; cleanup ticketed.

## Work completed

1. NVIDIA driver `595.91.07` + `nvidia-smi` healthy on host.
2. Installed NVIDIA Container Toolkit; `nvidia-ctk cdi generate` → `/etc/cdi/nvidia.yaml`; `cdi list` shows `nvidia.com/gpu=0|UUID|all`.
3. CDI unusable: Podman `unresolvable CDI devices nvidia.com/gpu=*`.
4. Working `ollama` compose wiring:
   - devices: `/dev/nvidia*`
   - volumes: host `libcuda.so.1`, `libnvidia-ml.so.1`, `libnvidia-ptxjitcompiler.so.1`, `nvidia-smi`
   - env: `NVIDIA_VISIBLE_DEVICES`, `NVIDIA_DRIVER_CAPABILITIES`, keep-alive / flash-attn
5. Verified: log line `library=CUDA … RTX 3090 … 24.0 GiB`; probe `qwen2.5:3b` offloaded **37/37** layers (~3.6 GiB VRAM).
6. Ecosystem filing:
   - Mirror: `deployment/workspace-compose.yml`
   - Ops docs: `deployment/README.md` GPU section
   - Host `ai/README.md` updated
   - Archive: `tickets/archive/2026-07-09-thelio-discrete-gpu-for-ollama.md`
   - New: `tickets/2026-09-16-podman-nvidia-cdi-cleanup.md`
   - Note on: `tickets/2026-07-17-version-workspace-compose-yml.md`

## Decisions

- Ship classic device+lib mounts now; treat CDI as P3 cleanup (driver upgrades will break versioned `.so` binds).
- Keep runtime compose at host `sawtooth/compose.yml`; version via ecosystem mirror until compose-ownership ticket closes.

## Open / next

- [ ] CDI cleanup ticket (`2026-09-16-podman-nvidia-cdi-cleanup`)
- [ ] Finish compose git-home decision (`2026-07-17-version-workspace-compose-yml`)
- [ ] Optional: raise Cromwell default model now that VRAM is available
- [ ] Resume Mode C / Wv2 paper Blue from WUT #685 (separate track)

## Links

- `deployment/workspace-compose.yml`
- `deployment/README.md`
- `docs/tickets/2026-09-16-podman-nvidia-cdi-cleanup.md`
